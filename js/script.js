'use strict';

let str = 'Daniil Abdulov Bilalovich';
let target = 'a';
let pos = 0;

while (true){
    let foundPos = str.indexOf(target, pos);
    if (foundPos == -1) break;
    if (foundPos){
        pos += 
    }
} 
console.log(pos);  
